TCP连接
  状态:
    established;
    listen;
    closed;
  行为:
    open();
    close();
    acknowledge();

需求: 状态不同, 行为逻辑不同

实现:
  v1版本: 不灵活, 修改容易出错; 一个类代码会越来越多, 不好维护
  v2版本: 更灵活

什么时候用?
  1. 一个对象, 行为随着状态的不同而不同;
  2. 而且行为不会新增扩展;

不适用?
  行为如果要扩展的话, 不适合, 因为IState接口要新增, 以前所有的接口都要新增

